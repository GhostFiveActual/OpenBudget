{
  "name": "openbudget",
  "version": "3.1.2",
  "private": false,
  "type": "module",
  "description": "A private, offline-first, open-source personal finance desktop application.",
  "main": "electron/main.cjs",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "desktop": "electron .",
    "desktop:dev": "concurrently -k \"vite --host 127.0.0.1\" \"wait-on http://127.0.0.1:5173 && cross-env OPENBUDGET_DEV=1 electron .\"",
    "dist": "npm run build && electron-builder",
    "dist:win": "npm run build && electron-builder --win",
    "dist:mac": "npm run build && electron-builder --mac",
    "dist:linux": "npm run build && electron-builder --linux",
    "test": "node tests/engine.test.mjs && node tests/store.test.mjs && node tests/release.test.mjs"
  },
  "devDependencies": {
    "concurrently": "^9.2.0",
    "cross-env": "^10.0.0",
    "electron": "^37.3.1",
    "electron-builder": "^26.0.12",
    "vite": "^7.1.3",
    "wait-on": "^8.0.4"
  },
  "build": {
    "appId": "org.openbudget.desktop",
    "productName": "OpenBudget",
    "asar": true,
    "files": [
      "dist/**/*",
      "electron/**/*",
      "LICENSE",
      "README.md",
      "SECURITY.md"
    ],
    "directories": {
      "output": "release"
    },
    "win": {
      "target": [
        "nsis"
      ],
      "artifactName": "OpenBudget-${version}-Windows-Setup.${ext}"
    },
    "nsis": {
      "oneClick": false,
      "allowToChangeInstallationDirectory": true,
      "createDesktopShortcut": true,
      "createStartMenuShortcut": true
    },
    "mac": {
      "target": [
        "dmg"
      ],
      "category": "public.app-category.finance",
      "artifactName": "OpenBudget-${version}-macOS.${ext}"
    },
    "linux": {
      "target": [
        "AppImage",
        "deb"
      ],
      "category": "Office",
      "artifactName": "OpenBudget-${version}-Linux-${arch}.${ext}"
    }
  },
  "license": "MIT"
}
